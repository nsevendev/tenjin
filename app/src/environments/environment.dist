export const environment = {
  // mettre true en prod ou preprod
  production: false,
  // host à changer en fonction de l'environement si dev laisser celui ci
  API_BASE: 'https://tenjin-api.local',
  // garde prefix de l'api, voir par la suite si bloquant car non flexible
  API_PREFIX: '/api/v1',
  // utiliser pour les cookies (true si cookies)
  WITH_CREDENTIALS: true,
};
